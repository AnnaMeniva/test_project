import s from "./ProfilePage.module.scss";

export const profileLoader = async () => {};

const ProfilePage: React.FC = () => {
  return <div className={s.profilePageWrapper}>My Profile</div>;
};

export default ProfilePage;
